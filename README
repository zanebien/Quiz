# Example:

// Configure quiz container
QuizContainer::setdb($pdoconn);

// Get Quiz
$quiz = QuizContainer::createQuiz();
$quiz->getById(1);

// Get Quiz Questions
$quiz->getQuestions();

// Randomize Order of Quiz Questions
$quiz->randomizeOrder();

// Display the quiz
$quiz->render();